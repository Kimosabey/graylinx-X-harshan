# Python
__pycache__/
*.py[cod]

# Tooling / local config
.claude/

# Design handoff (kept on disk, not tracked) + scratch
_handoff/
graylinx-X-harshan-handoff/
_drop/
_gitscan.json
*.zip

# Private Teams 1:1 DM exports — raw chats are build INPUTS only, never committed.
# Only the derived per-day "communications" topic strings flow into the seed.
gl_teams_chat/

# Netlify
.netlify/

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.swp
